SELECT JSON_UNQUOTE(JSON_EXTRACT(json_data, '$.account_id')) AS account_id, 
SUM(CAST(JSON_UNQUOTE(JSON_EXTRACT(json_data, '$.amount')) AS DECIMAL(10,2))) AS total_amount 
FROM transactions GROUP BY JSON_UNQUOTE(JSON_EXTRACT(json_data, '$.account_id'));